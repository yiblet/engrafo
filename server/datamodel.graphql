type User {
  id: ID! @unique
  email: String! @unique
  name: String!
}

type Paper {
  id: ID! @unique
  arxivId: String! @unique
  comments: [Comment!]!
}

type Comment {
  id: ID! @unique
  content: String!
  paper: Paper!
  poster: User!
}
